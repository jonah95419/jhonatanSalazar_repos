{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "RepositoryCreateRequest",
    "additionalProperties": false,
    "properties": {
        "id_tribe": {
            "type": "number"
        },
        "name": {
            "type": "string"
        },
        "state": {
            "enum": ["E", "D", "A"],
            "type": "string"
        },
        "status": {
            "enum": ["I", "A"],
            "type": "string"
        }
    },
    "required": [
        "id_repository",
        "id_tribe",
        "name",
        "state",
        "status"
    ]
}